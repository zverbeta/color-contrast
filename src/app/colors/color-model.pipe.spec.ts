import { ColorModelPipe } from './color-model.pipe';

describe('ColorModelPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorModelPipe();
    expect(pipe).toBeTruthy();
  });
});
